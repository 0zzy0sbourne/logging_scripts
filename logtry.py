
import logging
import os
import time

logging.basicConfig(filename="./logs/shortest_path.log", level=logging.DEBUG)
print("Shortest Path logging is started")
logging.debug(f"{time.asctime(time.localtime(time.time()))}: Shortest Path Logging")
